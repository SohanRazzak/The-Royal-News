import Header from "../Header/Header";
import Navbar from "../Navbar/Navbar";

const Profile = () => {
    return (
        <div>
            <Header/>
            <Navbar/>
            Profile
        </div>
    );
};

export default Profile;